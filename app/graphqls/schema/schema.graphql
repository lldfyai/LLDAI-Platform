type Query {
    problems(userId: Int!): [Problem]
    problem(problemId: Int!): Problem
}

type Problem {
    problemId: Int
    problemTitle: String
    difficulty: String
    tags: [String]
    timeLimit: Float
    memoryLimit: Float
    s3Path: String
}
